import { StatusLogger, StatusContract } from "./StatusLogger";
import { AgentLogger } from "./DataModel";
export declare class ConsoleStatusLogger implements StatusLogger {
    private _logger;
    FULL_PATH: string;
    constructor(_logger?: AgentLogger);
    isNodeVersionCompatible(): boolean;
    makeStatusDirs(filepath?: string): string;
    writeFile(data: StatusContract, cb?: Function): void;
    addCloseHandler(): void;
}
